import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    image: {
        width: 280,
        height: 280,
        borderWidth: 4,
        borderColor: "#0f18c4ff",
        borderRadius: 140
    }
});